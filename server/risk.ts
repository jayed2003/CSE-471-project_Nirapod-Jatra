export type RiskInputs = { aqi: number; floodStatus: "None" | "Watch" | "Warning"; dengueStatus?: "None" | "Watch" | "Warning"; weatherAlert?: string; unrestAlert?: string };
export function aqiLevel(aqi: number): "Low" | "Moderate" | "High" | "Severe" { if (aqi <= 50) return "Low"; if (aqi <= 100) return "Moderate"; if (aqi <= 150) return "High"; return "Severe"; }
function aqiAdvice(aqi: number) { const level = aqiLevel(aqi); if (level === "Low") return "Air quality is suitable for normal outdoor activity"; if (level === "Moderate") return "Sensitive travelers should limit extended outdoor activity"; if (level === "High") return "Reduce prolonged outdoor exertion"; return "Avoid outdoor exertion and use a filtered indoor space"; }
export function riskLevel(input: RiskInputs): "Low" | "Moderate" | "High" | "Severe" { if (input.floodStatus === "Warning" || input.dengueStatus === "Warning" || input.unrestAlert || input.aqi > 150) return "Severe"; if (input.floodStatus === "Watch" || input.dengueStatus === "Watch" || input.weatherAlert || input.aqi > 100) return "High"; if (input.aqi > 50) return "Moderate"; return "Low"; }
export function buildBrief(input: RiskInputs) { return `Overall risk: ${riskLevel(input)}. Flood risk: ${input.floodStatus}. Dengue risk: ${input.dengueStatus ?? "None"}. AQI: ${input.aqi} - ${aqiAdvice(input.aqi)}. Weather: ${input.weatherAlert ?? "No alerts"}. Unrest: ${input.unrestAlert ?? "No alerts"}.`; }
export function baselineRisk(): RiskInputs { return { aqi: 45, floodStatus: "None", dengueStatus: "None" }; }